# =============================================================================
# Senior Guardian System - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# For development with SQLite (default if not set):
# DATABASE_URL=sqlite:///./database.db
#
# For production with PostgreSQL:
DATABASE_URL=postgresql://postgres:password@localhost:5432/senior_guardian_db

# -----------------------------------------------------------------------------
# Security Configuration - MUST CHANGE IN PRODUCTION!
# -----------------------------------------------------------------------------
# Generate a secure key with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-super-secret-key-change-in-production-2024

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
# Enable debug mode (set to "true" for development, "false" for production)
DEBUG=false

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# CORS origins (comma-separated list for production)
# Leave empty to use development defaults
# CORS_ORIGINS=https://your-frontend-domain.com,https://another-domain.com

# -----------------------------------------------------------------------------
# LLM API Configuration
# -----------------------------------------------------------------------------
# DeepSeek API (optional - system will use mock analysis if not configured)
DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com

# OpenAI API (alternative, optional)
# OPENAI_API_KEY=your_openai_api_key
